{
  "name": "torre-technical-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "cd frontend && npm install && cd ..",
    "postinstall": "cd frontend && npm run build && cd ..",
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www"
  },
  "dependencies": {
    "cross-fetch": "^3.1.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
